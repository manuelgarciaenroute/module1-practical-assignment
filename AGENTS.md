# AGENTS

## Scope
- Default repository: `./moment-develop`.
- If a user mentions "the repo" or "repository" without an explicit path/name, interpret it as `./moment-develop`.

## Objective
- Analyze the repository to produce onboarding documentation.

## Findings Output
- Save every requested finding as a separate Markdown file under `./findings`.
- Use meaningful, descriptive filenames (for example: `architecture-overview.md`, `setup-steps.md`).